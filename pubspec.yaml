name: penta_core
description: "A new Flutter package project."
version: 1.0.0

environment:
  sdk: ^3.6.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  # Used for check the internet connection
  internet_connection_checker: ^3.0.1
  intl: ^0.20.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^7.0.0

flutter:
  uses-material-design: true
